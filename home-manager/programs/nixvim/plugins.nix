{
  plugins = {
    neo-tree = {
      enable = true;
    };
    # required for neo-tree (optional)
    web-devicons = {
      enable = true;
    };
    mini-surround = {
      enable = true;
    };
    blink-cmp = {
      enable = true;
    };
    indent-blankline = {
      enable = true;
    };
  };
}
