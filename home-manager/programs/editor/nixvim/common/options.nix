{
  opts = {
    number = true;
    # Enable cursor line highlight
    cursorline = true;

    # Disable swap files and enable undo files for persistent undo history
    swapfile = false;
    undofile = true;
    undolevels = 10000;

    list = true;
    listchars = "space:·";
  };
}
